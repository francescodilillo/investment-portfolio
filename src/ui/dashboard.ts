import {parsePortfolioConfig} from "../core/config";import {calculateDashboard} from "../core/calculations";import {parseTransactionsCsv} from "../core/parser";import {reconstructPortfolio} from "../core/portfolio";import type {DashboardModel} from "../core/types";import {formatCurrency,formatPercent} from "../utils/format";import {renderCharts} from "./charts";import {renderHoldingsTable,renderTransactionsTable} from "./tables";import {valueTone} from "./theme";
/** Mounts the local-only dashboard and file import workflow. */
export function mountDashboard(root:HTMLElement):void{root.className="app";root.replaceChildren(header(),onboarding());root.addEventListener("click",async e=>{if((e.target as HTMLElement).id!=="load")return;const csv=root.querySelector<HTMLInputElement>("#csv")?.files?.[0],yml=root.querySelector<HTMLInputElement>("#yml")?.files?.[0];if(!csv||!yml)return replace(root,onboarding("Choose both a transaction CSV and portfolio.yml."));try{const config=parsePortfolioConfig(await yml.text());const state=reconstructPortfolio(parseTransactionsCsv(await csv.text(),config),config);replace(root,view(calculateDashboard(state,config.currency)));}catch(error){replace(root,onboarding(error instanceof Error?error.message:"The files could not be loaded."));}});}
function header():HTMLElement{const h=document.createElement("header");h.innerHTML="<p>LOCAL-FIRST PORTFOLIO ANALYTICS</p><h1>Investment Dashboard</h1><small>Your selected files stay in this browser.</small>";return h;}function onboarding(error?:string):HTMLElement{const s=document.createElement("section");s.className="panel onboarding";s.innerHTML=`<h2>Load your local files</h2><p>Select a transaction CSV and portfolio.yml. Nothing is uploaded or stored.</p>${error?`<p class=error>${escape(error)}</p>`:""}<label>Transactions CSV<input id=csv type=file accept=".csv,text/csv"></label><label>portfolio.yml<input id=yml type=file accept=".yml,.yaml,text/yaml"></label><button id=load>Build dashboard</button>`;return s;}function replace(root:HTMLElement,c:HTMLElement):void{root.replaceChildren(header(),c);}function view(m:DashboardModel):HTMLElement{const d=document.createElement("div");d.className="dashboard";d.append(cards(m),renderCharts(m.allocation,m.topHoldings,m.currency),renderHoldingsTable(m.holdings,m.currency),renderTransactionsTable(m.transactions,m.currency));return d;}function cards(m:DashboardModel):HTMLElement{const x=m.metrics,items:[string,string,number][]=[["Portfolio Value",formatCurrency(x.portfolioValue,m.currency),0],["Cost Basis",formatCurrency(x.costBasis,m.currency),0],["Unrealized Gain (€)",formatCurrency(x.unrealizedGain,m.currency),x.unrealizedGain],["Unrealized Gain (%)",formatPercent(x.unrealizedGainPercent),x.unrealizedGainPercent],["Realized Gain",formatCurrency(x.realizedGain,m.currency),x.realizedGain],["Total Gain",formatCurrency(x.totalGain,m.currency),x.totalGain],["Net Invested Capital",formatCurrency(x.netInvestedCapital,m.currency),0],["Number of Holdings",String(x.numberOfHoldings),0]];const s=document.createElement("section");s.className="metrics";s.innerHTML=items.map(i=>`<article><p>${i[0]}</p><b class=${valueTone(i[2])}>${i[1]}</b></article>`).join("");return s;}function escape(v:string):string{const e=document.createElement("span");e.textContent=v;return e.innerHTML;}
